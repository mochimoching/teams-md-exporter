# MS Teams 会話履歴スクレイピングツール 仕様書

**版数**: v1.0
**作成日**: 2026-08-03
**対象**: 特別権限（Graph API / 管理者 / エクスポート機能）を持たない一般利用者
**前提ツール**: GitHub Copilot（コード生成）、Teams デスクトップアプリ、Teams Web アプリ

---

## 0. エグゼクティブサマリ

会社の Teams から、自分が閲覧できる特定のチャネル・スレッド・チャットの会話履歴を抽出し、Markdown ファイルとして保存するツールの仕様。API・管理者権限・公式エクスポート機能をいっさい使わず、**「画面に表示されている＝自分が正規に閲覧できる」情報だけ**を対象とする。

3方式（ブラウザ拡張／ユーザースクリプト、ブラウザ自動化、ローカルキャッシュ解析）を比較した結果、

> **推奨: Teams Web 上で動く「ブラウザ側抽出（ユーザースクリプト or 拡張機能）」を主方式とし、無人・定期実行が必要な場合のみ Playwright によるブラウザ自動化を補助方式とする。**

ローカルキャッシュ直接解析は、スキーマ非公開・暗号化・規約リスクの観点から**非推奨**とする。

---

## 1. 目的とスコープ

### 1.1 目的

- 自分がメンバーとして参加している、または閲覧権限のある Teams の会話（チャネルの投稿、返信スレッド、1:1／グループチャット）を、後から検索・参照・バックアップできる形で **Markdown** に保存する。
- 個人の議事メモ・ナレッジ整理・自己記録を目的とし、業務上正当にアクセスできる範囲に限定する。

### 1.2 スコープに含む

- 指定した1つのチャネル配下の投稿とその返信スレッド
- 指定した1つのチャット（1:1／グループ）
- メッセージ本文、送信者表示名、送信日時、返信の親子関係、リアクション（可能な範囲）、添付ファイル名とリンク（本文の一部として）
- コードブロック・引用・箇条書きなどの書式の Markdown への写像

### 1.3 スコープに含まない（非対象）

- 自分に閲覧権限のないチャネル／チャット
- 添付ファイルの実体ダウンロード（ファイル名・リンクのみ記録。実体取得はオプション扱いで別途検討）
- 音声・ビデオ通話の録画・文字起こし
- 全社横断の一括エクスポート（それは本来 eDiscovery / Graph API の領域であり、権限が前提）

### 1.4 明示的な制約（依頼内容より）

- Graph API・Teams Export API は使えない（今後も付与見込みなし）。
- 管理者権限・コンプライアンス機能（eDiscovery、コンテンツ検索）は使えない。
- したがって本ツールは、**利用者本人の認証済みセッションを通じて、UI に表示される情報を機械的に読み取る**アプローチしか取り得ない。

---

## 2. アプローチ比較と推奨

3つの現実的な実装方式を、制約下で比較する。

### 2.1 方式A: ブラウザ側抽出（ユーザースクリプト／拡張機能）★推奨（主方式）

Teams Web（`teams.microsoft.com`）を通常どおり自分でログインして開き、その**ページ内で動作する JavaScript**（Tampermonkey などのユーザースクリプト、または自作の Chrome 拡張のコンテンツスクリプト）が、表示中の会話の DOM を読み取ってメッセージを抽出する。仮想スクロール対策として、スクリプトが会話ペインを自動で上方向にスクロールしながら順次読み込み、重複排除しつつメッセージを蓄積する。

| 観点 | 評価 |
|---|---|
| 認証 | ◎ 既存のログインセッションをそのまま利用。MFA も利用者が普通にログインするだけ。資格情報をコードで扱わない。 |
| 権限 | ◎ ブラウザが表示できる＝自分が閲覧可能な範囲のみ。権限昇格は一切起こらない。 |
| 実装難易度 | ○ DOM セレクタの調査は要るが、Copilot と相性が良く、既存 OSS 実装も豊富。 |
| 保守性 | △ Teams Web の DOM 変更で壊れる（セレクタを外だしにして緩和）。 |
| 自動化・定期実行 | △ 手動で会話を開いてボタンを押す半自動運用が基本。 |
| 規約・コンプラリスク | ○ 追加の隠しデータへアクセスしない分、方式Cより穏当。ただし後述の注意は必要。 |

### 2.2 方式B: ブラウザ自動化（Playwright / Selenium）＝補助方式

Playwright（推奨）または Selenium で Teams Web を自動操作し、目的の会話を開いてスクロール＋抽出する。**永続化したブラウザプロファイル**（一度手動ログイン済みの user-data-dir）を使い、以降はセッション Cookie を再利用して再ログインを避ける。

| 観点 | 評価 |
|---|---|
| 認証 | ○ 初回は手動ログイン。以降は永続プロファイルで再利用。MFA を自動化しようとしない（する必要がない設計にする）。 |
| 権限 | ◎ 方式Aと同じく閲覧可能範囲のみ。 |
| 実装難易度 | △ 抽出ロジックは方式Aとほぼ共通だが、ブラウザ起動・待機制御・要素待ちの実装が増える。 |
| 保守性 | △ DOM 変更に弱いのは方式Aと同様＋自動操作フローの脆さが加わる。 |
| 自動化・定期実行 | ◎ 完全自動・スケジュール実行が可能。複数会話の一括処理向き。 |
| 規約・コンプラリスク | △ 「自動化されたアクセス」は規約上より慎重な扱いが必要。頻度・並列度を抑える配慮が要る。 |

### 2.3 方式C: デスクトップアプリのローカルキャッシュ解析＝非推奨

新 Teams（Teams 2.0 / WebView2 ベース）が端末に保存するローカルデータ（`%LOCALAPPDATA%\Packages\MSTeams_*\LocalCache\...` 配下の IndexedDB/LevelDB 等）を直接読み、メッセージを復元する。フォレンジック研究や `teams-decoder` のような OSS が存在する領域。

| 観点 | 評価 |
|---|---|
| 認証 | ◎ 不要（ログイン済みのローカルデータを読むだけ）。 |
| 権限 | ○ 自端末のキャッシュのみだが、UI 非表示分まで含み得る点は要注意。 |
| 実装難易度 | ✕ スキーマ非公開・バージョン依存・部分暗号化で非常に高い。 |
| 保守性 | ✕ Teams の更新で構造が変わると即破綻。 |
| 自動化 | ○ ファイル読み取りなので自動化自体は容易。 |
| 規約・コンプラリスク | ✕ 「非公開の内部保存構造への直接アクセス」は規約・情報管理ポリシー上もっともグレー。**業務端末では特に避けるべき。** |

### 2.4 推奨結論

- **主方式 = 方式A（ブラウザ側抽出）**。認証・権限の扱いが最も素直で、資格情報を一切コードで扱わず、「見えているものだけ」という原則を技術的に担保できる。
- **補助方式 = 方式B（Playwright）**。複数会話の一括取得や定期バックアップが必要になった場合のみ採用。抽出コア（DOM→中間データ）は方式Aと共有する設計にする。
- **方式C は採用しない。**

以降の詳細仕様は、**抽出コアを共通化し、方式A/Bのどちらでも同じコアを呼べる**構成を前提に記述する。

---

## 3. 全体アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│  実行環境: 利用者のブラウザ（Teams Web を開いた状態）             │
│                                                             │
│  ┌───────────────┐   ┌────────────────────┐   ┌───────────┐  │
│  │ ① スクロール     │→ │ ② 抽出コア          │→ │ ③ 正規化    │  │
│  │   ドライバ       │   │  DOM → 生メッセージ  │   │  中間JSON  │  │
│  │ (仮想スクロール   │   │  (セレクタ設定で駆動)│   │  モデル化   │  │
│  │  制御・重複排除)  │   └────────────────────┘   └─────┬─────┘  │
│  └───────────────┘                                    │        │
│                                                        ▼        │
│                                        ┌────────────────────┐   │
│                                        │ ④ Markdown レンダラ  │   │
│                                        └─────────┬──────────┘   │
└──────────────────────────────────────────────────┼────────────┘
                                                    ▼
                                    ⑤ ダウンロード（.md ファイル）
```

### 3.1 コンポーネント

1. **スクロールドライバ** — 会話ペインを検出し、上方向へ段階スクロールして過去メッセージを遅延ロードさせる。読み込み完了を検知しつつ、`data-*` の識別子で重複を排除しながら DOM ノードを収集する。
2. **抽出コア** — 収集した各メッセージ DOM から、送信者・日時・本文 HTML・返信関係・リアクション・添付を取り出す。**セレクタは外部設定ファイル**に切り出し、DOM 変更時にコードを触らず追随できるようにする。
3. **正規化** — 抽出結果を後述の中間データモデル（JSON）に整形。日時の ISO 8601 化、本文 HTML の Markdown 化前処理、スレッド親子の解決を行う。
4. **Markdown レンダラ** — 中間モデルから最終 Markdown を生成（§6 のフォーマット）。
5. **出力** — ブラウザの `Blob` + `a[download]` で `.md` を保存。方式Bではファイルシステムに直接書き出す。

### 3.2 設計原則

- **抽出コアとレンダラを疎結合に**（中間モデルを挟む）。将来 JSON/CSV 出力を足すのも中間モデルから分岐するだけ。
- **セレクタ外だし**。`selectors.json` のようにまとめ、Teams の UI 更新に対して設定変更で対応。
- **冪等・再開可能**。同一会話の再取得で重複しないよう、メッセージIDで一意化。中断しても途中まで保存できる。
- **秘密情報を持たない**。ID/パスワードをコードや設定に置かない（利用者の生ログインに委ねる）。

---

## 4. 抽出対象と識別方法

Teams Web は React ベースで、多くの要素に `data-tid` 属性が付与されている（例: メッセージ本文、送信者、タイムスタンプ）。ただし属性名・構造は**バージョンで変わり得る**ため、実装時に実DOMを調査して `selectors.json` を確定させる前提とする。

抽出単位ごとに取得したい情報:

| 対象 | 取得項目 |
|---|---|
| チャネル投稿（親） | メッセージID、送信者表示名、送信日時、本文、件名（あれば）、リアクション、添付名/リンク、返信件数 |
| 返信（スレッド子） | メッセージID、親メッセージID、送信者、日時、本文、リアクション、添付 |
| チャットメッセージ | メッセージID、送信者、日時、本文、リアクション、添付、編集/削除フラグ（可能なら） |
| システムメッセージ | 種別（参加/退出/名称変更等）、日時（任意で取得。既定は本文として控えめに記録 or 除外を選択可） |

> 注: メッセージIDは DOM 上の一意属性（`data-mid` や内部ID相当）を優先利用し、取得できない場合は「送信者＋日時＋本文ハッシュ」で代替キーを生成して重複排除する。

---

## 5. 中間データモデル（JSON）

Markdown 生成前の共通中間形式。将来の別形式出力にも再利用する。

```json
{
  "source": {
    "kind": "channel | chat",
    "team": "チーム名（channel時）",
    "channel": "チャネル名（channel時）",
    "chatTitle": "チャット名（chat時）",
    "url": "会話のURL",
    "capturedAt": "2026-08-03T09:00:00+09:00",
    "capturedBy": "利用者表示名",
    "toolVersion": "1.0"
  },
  "participants": [
    { "displayName": "山田 太郎" }
  ],
  "messages": [
    {
      "id": "1699999999999",
      "parentId": null,
      "author": "山田 太郎",
      "timestamp": "2026-08-01T14:23:00+09:00",
      "editedAt": null,
      "deleted": false,
      "bodyMarkdown": "本文（Markdown化済み）",
      "reactions": [ { "type": "like", "count": 3 } ],
      "attachments": [
        { "name": "設計書.docx", "url": "https://.../設計書.docx" }
      ],
      "system": false
    }
  ],
  "stats": {
    "messageCount": 128,
    "threadCount": 12,
    "rangeStart": "2026-06-01T09:00:00+09:00",
    "rangeEnd": "2026-08-01T18:00:00+09:00",
    "truncated": false
  }
}
```

- `parentId` により親投稿と返信の階層を表現。チャットでは基本 `null`（Teams チャットはフラット）。
- `truncated` は、スクロールで遡れる上限に達したり途中で打ち切った場合に `true` とし、Markdown 冒頭にも警告を出す（**サイレントな取りこぼしを作らない**）。

---

## 6. 出力 Markdown フォーマット

### 6.1 ファイル命名規則

```
teams_{kind}_{safeTitle}_{YYYYMMDD-HHmm}.md
例) teams_channel_開発チーム-一般_20260803-0900.md
    teams_chat_田中-佐藤-山田_20260803-0900.md
```

`safeTitle` はファイル名に使えない文字を `-` に置換。

### 6.2 文書構造

先頭に YAML フロントマター（メタ情報）を置き、本文は時系列。返信はインデント付き引用で親にぶら下げる。

````markdown
---
source_kind: channel
team: 開発チーム
channel: 一般
url: https://teams.microsoft.com/l/channel/...
captured_at: 2026-08-03T09:00:00+09:00
captured_by: 田山 大輝
message_count: 128
range: 2026-06-01 〜 2026-08-01
truncated: false
tool_version: "1.0"
---

# 開発チーム / 一般（チャネル）

> このファイルは Teams Web の表示内容から自動抽出したものです。抽出時点: 2026-08-03 09:00 (JST)

## 2026-08-01 (金)

### 14:23  山田 太郎
リリース手順を更新しました。確認お願いします。
📎 添付: [リリース手順.docx](https://.../リリース手順.docx)
👍 3  🎉 1

> #### 14:31  佐藤 花子 ↳返信
> 確認しました。1点だけ、手順3の順序が…
>
> #### 14:45  山田 太郎 ↳返信
> ご指摘ありがとうございます。修正しました。

### 15:02  田中 一郎
```powershell
Deploy-Release -Env prod
```
本番反映済みです。
````

### 6.3 書式の写像ルール

- 太字・斜体・取り消し線・インラインコード・コードブロック・箇条書き・番号付き・引用・リンクは、対応する Markdown へ変換。
- Teams の絵文字/カスタム絵文字はショートコードまたは Unicode に落とす（不明なものは `:name:` 表記で温存）。
- メンション `@山田太郎` は `**@山田太郎**` として強調。
- リアクションは各メッセージ末尾に「絵文字＋件数」で1行。
- 添付は本文直後に `📎 添付: [ファイル名](URL)` 行として列挙（実体はダウンロードしない既定）。
- 編集済みは日時の後ろに `(編集済み)`、削除済みは `(このメッセージは削除されました)` と表示。
- 変換不能な要素は捨てずに、HTMLコメント `<!-- 未対応要素: card -->` で存在を残す。

### 6.4 分割・上限

- 1ファイルが極端に大きくなる場合（例: 5MB / 5,000メッセージ超）、日付境界で `part1/part2` に分割するオプションを設ける。分割時は各ファイルのフロントマターに `part` と `part_of` を記録。

---

## 7. 処理フロー（主方式A）

1. 利用者が Teams Web で対象の会話（チャネル or チャット）を開く。
2. ツールUI（ユーザースクリプトが挿す小さなパネル、または拡張のポップアップ）で「取得開始」を押す。取得範囲（全件 / 直近N日 / 日付指定）と、システムメッセージ除外可否を選ぶ。
3. スクロールドライバが会話ペイン先頭まで段階的に遡り、遅延ロードを待ちながらメッセージノードを収集（重複排除・進捗表示）。
4. 抽出コアが各ノードを解析 → 正規化して中間モデルを構築。
5. Markdown レンダラが `.md` を生成し、自動ダウンロード。
6. 完了トースト（取得件数・期間・truncated 有無）を表示。

### 7.1 補助方式B（Playwright）の差分

- 起動時に永続プロファイルを指定 → 未ログインなら手動ログインを促して待機 → ログイン後にセッション再利用。
- 会話URLへ直接 `goto` → 会話ペインを待機 → 以降は同じ抽出コアを呼ぶ。
- 複数URLをリスト入力し、順次処理（**低頻度・逐次**でアクセス、乱数ウェイトを挟む）。
- 出力はローカルの指定フォルダへ `.md` を直接書き込み。

---

## 8. 技術スタックと GitHub Copilot の活用

| 領域 | 推奨技術 |
|---|---|
| 主方式A | Vanilla JS または TypeScript。配布はユーザースクリプト（Tampermonkey）または Chrome/Edge 拡張（Manifest V3、content script）。 |
| 補助方式B | Node.js + Playwright（TypeScript）。永続 `userDataDir` でセッション再利用。 |
| Markdown 生成 | 自作の軽量レンダラ（依存を減らす）。HTML→MD 変換は必要に応じ `turndown` 等を検討。 |
| 設定 | `selectors.json`（DOM セレクタ）、`config.json`（範囲・出力オプション）。 |

**Copilot の使いどころ**: DOM セレクタ探索の補助、正規化ロジックとエッジケース（絵文字・コードブロック・メンション）の実装、Playwright の待機処理の定型化、単体テストの雛形生成。仕様のうち §5 中間モデルと §6 フォーマットを Copilot への指示（コメント/型定義）として与えると精度が上がる。

---

## 9. エラー処理・エッジケース

- **仮想スクロールの取りこぼし**: スクロール後に一定時間ノード数が増えなければ「先頭到達」と判定。ただし急スクロールで飛ばさないよう小刻みに。取り切れなかった疑いがある場合は `truncated: true`。
- **DOM 変更でセレクタ不一致**: 抽出0件や必須項目欠落を検知したら即エラー表示し、`selectors.json` 更新を促す（黙って空ファイルを出さない）。
- **長大会話**: 進捗表示・中断/再開・分割出力で対応。
- **同名送信者・システムメッセージ**: 種別で分類、既定でシステムメッセージは控えめ表示 or 除外を選択可能。
- **日時のタイムゾーン**: 利用者ローカル（Asia/Tokyo, UTC+9）で ISO 8601（オフセット付き）に統一。
- **レート/負荷**: 方式Bでは会話間にランダムウェイト、並列実行はしない。

---

## 10. コンプライアンス・運用上の注意（重要）

本ツールは技術的には「自分が閲覧できる範囲の画面情報の書き出し」に過ぎないが、**職場の Teams が対象である以上、実装前に必ず次を確認・順守すること。**

- **勤務先の利用規程・情報管理ポリシー**: 会話履歴の外部保存・自動抽出が許可されているか。機密区分（社外秘・個人情報等）のデータをローカルに落とす行為が規程に反しないか。多くの企業では業務データの持ち出し・自動化アクセスに制限がある。
- **Microsoft の利用規約**: 自動化・スクレイピングを制限する条項があり得る。特に方式B（自動化アクセス）はより慎重に。少なくとも自分の閲覧権限を越えない、過度な負荷をかけない、を守る。
- **他者のプライバシー**: 会話には他人の発言・個人情報が含まれる。保存物の取り扱い（暗号化・保管場所・共有範囲・保存期間）を自分で管理する。不要になったら削除。
- **目的の正当性**: 自分の業務記録・ナレッジ整理など正当な目的に限る。監視・無断収集・持ち出し目的では使わない。
- **推奨運用**: 迷ったら情報システム部門やコンプライアンス窓口に事前確認する。まずは方式A（手動起動）で最小範囲から始め、必要性が明確な場合のみ方式Bへ拡張する。

> このセクションは仕様の一部です。実装の可否判断より前に、上記の規程確認を実務上の前提条件（ゲート）として扱ってください。

---

## 11. 段階的実装ロードマップ

1. **PoC（半日〜1日）**: Teams Web を開き、ブラウザのコンソールに貼る短いスクリプトで、**現在表示中の**メッセージを抽出しコンソール出力。`selectors.json` の初版を確定。
2. **抽出コア＋正規化（1〜2日）**: 中間モデル（§5）を実装。コードブロック・メンション・リアクション・添付のエッジケースを Copilot で詰める。
3. **スクロールドライバ（1日）**: 仮想スクロール制御と重複排除、進捗・truncated 判定。
4. **Markdown レンダラ＋ダウンロード（1日）**: §6 のフォーマットで `.md` 出力。フロントマター・分割対応。
5. **配布形態（0.5〜1日）**: ユーザースクリプト or 拡張にパッケージ化。設定パネルUI。
6. **（任意）方式B化（1〜2日）**: 抽出コアを Playwright から呼べるよう分離し、永続プロファイル＋逐次処理で複数会話一括対応。

---

## 12. 受け入れ基準（Definition of Done）

- 指定した1チャネルの投稿と返信スレッドが、親子関係を保ったまま Markdown 1ファイルに出力される。
- 指定した1チャットの全メッセージが時系列で出力される。
- 送信者・日時・本文・コードブロック・添付名/リンク・リアクションが正しく写像される。
- スクロール上限到達や途中打ち切りが `truncated` とファイル冒頭警告で明示される。
- DOM 変更時、コードを触らず `selectors.json` の修正だけで復旧できる。
- 認証情報をコード・設定に一切保持していない。
- §10 の注意事項が README とツールUIの双方に明記されている。

---

## 付録A: `selectors.json`（雛形イメージ）

```json
{
  "conversationPane": "[data-tid='message-pane'], [role='log']",
  "messageItem": "[data-tid='chat-pane-message'], [data-tid='channel-pane-message']",
  "author": "[data-tid='message-author-name']",
  "timestamp": "time, [data-tid='message-timestamp']",
  "body": "[data-tid='message-body-content']",
  "reaction": "[data-tid='reaction-badge']",
  "attachment": "[data-tid='file-attachment'] a",
  "replyContainer": "[data-tid='reply-list']"
}
```

> 上記の属性値は例示。実装時に実際の Teams Web の DOM を調査して確定・維持する。

## 付録B: 参考にした既存アプローチ

同種の実装が公開されており、方式A/B/Cいずれも実在することを確認済み。設計の裏付け・セレクタ調査の参考にできる（利用時は各ライセンスと自社規程を確認）。

- ブラウザ拡張でTeams Webのチャットをエクスポートする実装（方式A）
- SeleniumでTeamsチャネルを抽出する実装（方式B）
- ローカルクライアントからメッセージを復元する実装／フォレンジック研究（方式C・非推奨の裏付け）
