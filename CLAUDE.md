# CLAUDE.md — Teams 会話履歴 Markdown エクスポータ（方式A）

このリポジトリは、権限（Graph API / 管理者）を持たない前提で、**Teams Web に表示されている会話だけ**を
DOM から抽出し、Markdown に保存するツール。実装方針は同梱の `teams-scraper-spec.md`（仕様書）に従う。

## 絶対原則（逸脱しない）

1. **見えているものだけ**。利用者本人の認証済みセッションで表示される情報のみを対象にする。
   隠しデータ・非公開の内部保存（ローカルキャッシュ/IndexedDB等）へはアクセスしない（方式C禁止）。
2. **認証情報を持たない**。ID/パスワード/トークンをコード・設定・ログに一切保存しない。ログインは人が行う。
3. **セレクタは外だし**。DOM 依存の記述は `selectors.json` に集約し、UI 変更に設定変更で追随する。
   コード中にセレクタをハードコードしない。
4. **サイレントな取りこぼしを作らない**。スクロール上限到達や打ち切りは `truncated=true` とし、
   出力 Markdown の冒頭にも警告を出す。抽出0件や必須項目欠落は「成功」にせず明確にエラーにする。
5. **機密の扱い**。テストは低機密の会話で行う。実データ保存前に勤務先の情報管理規程を確認する（仕様書§10）。

## アーキテクチャ（疎結合を保つ）

`スクロールドライバ → 抽出コア(DOM→生) → 正規化(中間JSONモデル) → Markdownレンダラ → ダウンロード`

- 中間データモデルは仕様書 §5、出力フォーマットは §6 を正とする。
- 抽出コアは方式A（ブラウザ内）でも将来の方式B（Playwright）でも同じものを呼べるよう、
  DOM ルート要素を引数で受け取る純粋関数として実装する。

## 技術スタック

- 出発点はユーザースクリプト（Tampermonkey）。まずは Vanilla JS / TypeScript。
- HTML→Markdown 変換は自作の軽量ロジックを基本とし、必要なら turndown を検討。
- 依存は最小限に。

## 実装の進め方

1. `docs/dom-samples/` に、人間が DevTools で採取した実際のメッセージDOMのHTMLを置く。
   まずそれを読んで `selectors.json` を確定する（推測でセレクタを書かない）。
2. 抽出コア→正規化→レンダラの順に、単体で確認しながら組む。
3. エッジケース（コードブロック / メンション / リアクション / 添付 / 編集済み・削除済み / システムメッセージ）を
   仕様書 §6.3・§9 に沿って処理する。

## やらないこと

- 添付ファイルの実体ダウンロード（既定では名前とリンクのみ）。
- 自動ログイン・MFA自動化。
- 過度な並列アクセスや高頻度リクエスト。
